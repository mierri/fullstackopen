{
  "name": "phonebook-back",
  "version": "1.0.0",
  "description": "",
  "author": "mafer",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../../part/phonebook && npm run build && cp -r dist ../../part3/phonebook-back",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.11.0"
  }
}
